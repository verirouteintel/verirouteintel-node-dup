# Moved

This repository was created in error and is no longer used.

The official VeriRoute Intel Node/TypeScript SDK lives at
**[verirouteintel/verirouteintel-node](https://github.com/verirouteintel/verirouteintel-node)**
and is published to npm as [`verirouteintel`](https://www.npmjs.com/package/verirouteintel).

The hosted MCP server lives at
**[verirouteintel/veriroute-intel-mcp](https://github.com/verirouteintel/veriroute-intel-mcp)**.
